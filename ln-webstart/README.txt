To build jar:
    mvn package
    
To build windows executable:
    mvn package
    
To build mac os x app:
    mvn package appbundle:bundle
    

The jar and the windows executable will be in the target directory and the mac 
app bundle will be under target/ln-toolkit-1.0.0
